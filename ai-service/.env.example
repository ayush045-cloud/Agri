# ── AI Service Environment Variables ────────────────────────────────────────
# Port for the FastAPI service
PORT=8000

# Path where trained model files are stored
MODEL_DIR=./models

# Optional: log level (debug, info, warning, error)
LOG_LEVEL=info
